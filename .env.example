# Server configuration
PORT=3000
NODE_ENV=development

# Contact form: optional notification email (integration point)
CONTACT_NOTIFY_EMAIL=

# Rate limiting (requests per window per IP)
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=30
